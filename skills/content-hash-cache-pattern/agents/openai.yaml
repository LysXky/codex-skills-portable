interface:
  display_name: "Content Hash Cache Pattern"
  short_description: "Cache expensive file processing results using SHA-256 content..."
  default_prompt: "Use $content-hash-cache-pattern to help with this task."
policy:
  allow_implicit_invocation: true
