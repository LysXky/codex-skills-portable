interface:
  display_name: "Safety Guard"
  short_description: "Use this skill to prevent destructive operations when working..."
  default_prompt: "Use $safety-guard to help with this task."
policy:
  allow_implicit_invocation: true
