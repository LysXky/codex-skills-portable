interface:
  display_name: "Deployment Patterns"
  short_description: "Deployment workflows, CI/CD pipeline patterns, Docker contain..."
  default_prompt: "Use $deployment-patterns to help with this task."
policy:
  allow_implicit_invocation: true
