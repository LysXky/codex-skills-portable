interface:
  display_name: "Nutrient Document Processing"
  short_description: "Process, convert, OCR, extract, redact, sign, and fill docume..."
  default_prompt: "Use $nutrient-document-processing to help with this task."
policy:
  allow_implicit_invocation: true
