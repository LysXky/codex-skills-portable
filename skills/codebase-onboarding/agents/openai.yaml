interface:
  display_name: "Codebase Onboarding"
  short_description: "Analyze an unfamiliar codebase and generate a structured onbo..."
  default_prompt: "Use $codebase-onboarding to help with this task."
policy:
  allow_implicit_invocation: true
