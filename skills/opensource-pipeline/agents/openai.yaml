interface:
  display_name: "Opensource Pipeline"
  short_description: "Open-source pipeline: fork, sanitize, and package private pro..."
  default_prompt: "Use $opensource-pipeline to help with this task."
policy:
  allow_implicit_invocation: true
