interface:
  display_name: "Recursive Decision Ledger"
  short_description: "Use when the user asks for repeated rollouts, marked decision..."
  default_prompt: "Use $recursive-decision-ledger to help with this task."
policy:
  allow_implicit_invocation: true
