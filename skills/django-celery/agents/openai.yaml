interface:
  display_name: "Django Celery"
  short_description: "Django + Celery async task patterns — configuration, task des..."
  default_prompt: "Use $django-celery to help with this task."
policy:
  allow_implicit_invocation: true
