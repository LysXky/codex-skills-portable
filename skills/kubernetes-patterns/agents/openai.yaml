interface:
  display_name: "Kubernetes Patterns"
  short_description: "Kubernetes workload patterns, resource management, RBAC, prob..."
  default_prompt: "Use $kubernetes-patterns to help with this task."
policy:
  allow_implicit_invocation: true
