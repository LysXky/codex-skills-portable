interface:
  display_name: "Cost Tracking"
  short_description: "Track and report Claude Code token usage, spending, and budge..."
  default_prompt: "Use $cost-tracking to help with this task."
policy:
  allow_implicit_invocation: true
