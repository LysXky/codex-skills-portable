interface:
  display_name: "Springboot Verification"
  short_description: "Verification loop for Spring Boot projects: build, static ana..."
  default_prompt: "Use $springboot-verification to help with this task."
policy:
  allow_implicit_invocation: true
