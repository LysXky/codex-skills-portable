interface:
  display_name: "Returns Reverse Logistics"
  short_description: "Codified expertise for returns authorization, receipt and ins..."
  default_prompt: "Use $returns-reverse-logistics to help with this task."
policy:
  allow_implicit_invocation: true
