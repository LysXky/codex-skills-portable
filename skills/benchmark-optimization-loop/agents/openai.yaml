interface:
  display_name: "Benchmark Optimization Loop"
  short_description: "Use when the user asks to make something faster, try many var..."
  default_prompt: "Use $benchmark-optimization-loop to help with this task."
policy:
  allow_implicit_invocation: true
