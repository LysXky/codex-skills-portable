interface:
  display_name: "Dynamic Workflow Mode"
  short_description: "Design task-local harnesses, eval gates, and reusable skill e..."
  default_prompt: "Use $dynamic-workflow-mode to help with this task."
policy:
  allow_implicit_invocation: true
