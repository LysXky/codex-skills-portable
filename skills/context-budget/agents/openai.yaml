interface:
  display_name: "Context Budget"
  short_description: "Audits Claude Code context window consumption across agents..."
  default_prompt: "Use $context-budget to help with this task."
policy:
  allow_implicit_invocation: true
