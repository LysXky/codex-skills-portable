interface:
  display_name: "Plan Orchestrate"
  short_description: "Read a plan document, decompose it into steps, design a per-s..."
  default_prompt: "Use $plan-orchestrate to help with this task."
policy:
  allow_implicit_invocation: true
