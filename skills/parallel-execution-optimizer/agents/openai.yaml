interface:
  display_name: "Parallel Execution Optimizer"
  short_description: "Use when the user wants a task done much faster through paral..."
  default_prompt: "Use $parallel-execution-optimizer to help with this task."
policy:
  allow_implicit_invocation: true
