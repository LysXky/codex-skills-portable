interface:
  display_name: "Windows Desktop E2E"
  short_description: "E2E testing for Windows native desktop apps (WPF, WinForms, W..."
  default_prompt: "Use $windows-desktop-e2e to help with this task."
policy:
  allow_implicit_invocation: true
