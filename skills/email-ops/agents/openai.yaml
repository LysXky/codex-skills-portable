interface:
  display_name: "Email Ops"
  short_description: "Evidence-first mailbox triage, drafting, send verification, a..."
  default_prompt: "Use $email-ops to help with this task."
policy:
  allow_implicit_invocation: true
