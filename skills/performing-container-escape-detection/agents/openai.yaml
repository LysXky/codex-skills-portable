interface:
  display_name: "Performing Container Escape Detection"
  short_description: "Detects container escape attempts by analyzing namespace conf..."
  default_prompt: "Use $performing-container-escape-detection for an authorized cybersecurity task."
policy:
  allow_implicit_invocation: false
