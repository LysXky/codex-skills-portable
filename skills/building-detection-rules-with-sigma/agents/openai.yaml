interface:
  display_name: "Building Detection Rules With Sigma"
  short_description: "Builds vendor-agnostic detection rules using the Sigma rule f..."
  default_prompt: "Use $building-detection-rules-with-sigma for an authorized cybersecurity task."
policy:
  allow_implicit_invocation: false
