interface:
  display_name: "Workspace Surface Audit"
  short_description: "Audit the active repo, MCP servers, plugins, connectors, env..."
  default_prompt: "Use $workspace-surface-audit to help with this task."
policy:
  allow_implicit_invocation: true
