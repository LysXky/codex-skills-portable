interface:
  display_name: "Quarkus Verification"
  short_description: "Verification loop for Quarkus projects: build, static analysi..."
  default_prompt: "Use $quarkus-verification to help with this task."
policy:
  allow_implicit_invocation: true
