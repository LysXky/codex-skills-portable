interface:
  display_name: "Docker Patterns"
  short_description: "Docker and Docker Compose patterns for local development, con..."
  default_prompt: "Use $docker-patterns to help with this task."
policy:
  allow_implicit_invocation: true
