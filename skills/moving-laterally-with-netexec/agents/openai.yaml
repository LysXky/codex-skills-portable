interface:
  display_name: "Moving Laterally With Netexec"
  short_description: "Use NetExec for SMB, WinRM, LDAP, and MSSQL enumeration, pass..."
  default_prompt: "Use $moving-laterally-with-netexec for an authorized cybersecurity task."
policy:
  allow_implicit_invocation: false
