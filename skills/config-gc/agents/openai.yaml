interface:
  display_name: "Config Gc"
  short_description: "Garbage collection for your Claude Code configuration. Period..."
  default_prompt: "Use $config-gc to help with this task."
policy:
  allow_implicit_invocation: true
