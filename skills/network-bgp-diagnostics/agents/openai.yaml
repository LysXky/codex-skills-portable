interface:
  display_name: "Network Bgp Diagnostics"
  short_description: "Diagnostics-only BGP troubleshooting patterns for neighbor st..."
  default_prompt: "Use $network-bgp-diagnostics to help with this task."
policy:
  allow_implicit_invocation: true
