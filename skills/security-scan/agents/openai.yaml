interface:
  display_name: "Security Scan"
  short_description: "Scan your Claude Code configuration (.claude/ directory) for..."
  default_prompt: "Use $security-scan to help with this task."
policy:
  allow_implicit_invocation: true
