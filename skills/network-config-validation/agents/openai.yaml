interface:
  display_name: "Network Config Validation"
  short_description: "Pre-deployment checks for router and switch configuration, in..."
  default_prompt: "Use $network-config-validation to help with this task."
policy:
  allow_implicit_invocation: true
