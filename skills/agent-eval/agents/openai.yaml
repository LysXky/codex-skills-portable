interface:
  display_name: "Agent Eval"
  short_description: "Head-to-head comparison of coding agents (Claude Code, Aider..."
  default_prompt: "Use $agent-eval to help with this task."
policy:
  allow_implicit_invocation: true
