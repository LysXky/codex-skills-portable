interface:
  display_name: "Kotlin Exposed Patterns"
  short_description: "JetBrains Exposed ORM patterns including DSL queries, DAO pat..."
  default_prompt: "Use $kotlin-exposed-patterns to help with this task."
policy:
  allow_implicit_invocation: true
