interface:
  display_name: "Messages Ops"
  short_description: "Evidence-first live messaging workflow for ECC. Use when the..."
  default_prompt: "Use $messages-ops to help with this task."
policy:
  allow_implicit_invocation: true
