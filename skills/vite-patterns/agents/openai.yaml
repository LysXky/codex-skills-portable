interface:
  display_name: "Vite Patterns"
  short_description: "Vite build tool patterns including config, plugins, HMR, env..."
  default_prompt: "Use $vite-patterns to help with this task."
policy:
  allow_implicit_invocation: true
