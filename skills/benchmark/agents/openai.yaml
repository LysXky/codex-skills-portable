interface:
  display_name: "Benchmark"
  short_description: "Use this skill to measure performance baselines, detect regre..."
  default_prompt: "Use $benchmark to help with this task."
policy:
  allow_implicit_invocation: true
