interface:
  display_name: "Detecting Lateral Movement With Splunk"
  short_description: "Detect adversary lateral movement across networks using Splun..."
  default_prompt: "Use $detecting-lateral-movement-with-splunk for an authorized cybersecurity task."
policy:
  allow_implicit_invocation: false
