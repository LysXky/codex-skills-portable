interface:
  display_name: "Foundation Models On Device"
  short_description: "Apple FoundationModels framework for on-device LLM — text gen..."
  default_prompt: "Use $foundation-models-on-device to help with this task."
policy:
  allow_implicit_invocation: true
