interface:
  display_name: "Network Interface Health"
  short_description: "Diagnose interface errors, drops, CRCs, duplex mismatches, fl..."
  default_prompt: "Use $network-interface-health to help with this task."
policy:
  allow_implicit_invocation: true
