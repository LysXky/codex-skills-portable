interface:
  display_name: "Mysql Patterns"
  short_description: "MySQL and MariaDB schema, query, indexing, transaction, repli..."
  default_prompt: "Use $mysql-patterns to help with this task."
policy:
  allow_implicit_invocation: true
