interface:
  display_name: "Iterative Retrieval"
  short_description: "Pattern for progressively refining context retrieval to solve..."
  default_prompt: "Use $iterative-retrieval to help with this task."
policy:
  allow_implicit_invocation: true
