interface:
  display_name: "Claude Devfleet"
  short_description: "Orchestrate multi-agent coding tasks via Claude DevFleet — pl..."
  default_prompt: "Use $claude-devfleet to help with this task."
policy:
  allow_implicit_invocation: true
