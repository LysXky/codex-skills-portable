interface:
  display_name: "Postgres Patterns"
  short_description: "PostgreSQL database patterns for query optimization, schema d..."
  default_prompt: "Use $postgres-patterns to help with this task."
policy:
  allow_implicit_invocation: true
