interface:
  display_name: "Repo Scan"
  short_description: "Cross-stack source code asset audit — classifies every file..."
  default_prompt: "Use $repo-scan to help with this task."
policy:
  allow_implicit_invocation: true
