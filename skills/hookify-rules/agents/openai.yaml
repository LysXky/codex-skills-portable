interface:
  display_name: "Hookify Rules"
  short_description: "This skill should be used when the user asks to create a hook..."
  default_prompt: "Use $hookify-rules to help with this task."
policy:
  allow_implicit_invocation: true
