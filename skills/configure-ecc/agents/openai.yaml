interface:
  display_name: "Configure Ecc"
  short_description: "Interactive installer for Everything Claude Code — guides use..."
  default_prompt: "Use $configure-ecc to help with this task."
policy:
  allow_implicit_invocation: true
