interface:
  display_name: "Homelab Pihole Dns"
  short_description: "Pi-hole installation, blocklist management, DNS-over-HTTPS se..."
  default_prompt: "Use $homelab-pihole-dns to help with this task."
policy:
  allow_implicit_invocation: true
