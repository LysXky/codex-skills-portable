interface:
  display_name: "Detecting Suspicious Oauth Application Consent"
  short_description: "Detect risky OAuth application consent grants in Azure AD / M..."
  default_prompt: "Use $detecting-suspicious-oauth-application-consent for an authorized cybersecurity task."
policy:
  allow_implicit_invocation: false
