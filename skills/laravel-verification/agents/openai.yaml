interface:
  display_name: "Laravel Verification"
  short_description: "Verification loop for Laravel projects: env checks, linting..."
  default_prompt: "Use $laravel-verification to help with this task."
policy:
  allow_implicit_invocation: true
