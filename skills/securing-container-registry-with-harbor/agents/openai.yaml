interface:
  display_name: "Securing Container Registry With Harbor"
  short_description: "Harbor is an open-source container registry that provides sec..."
  default_prompt: "Use $securing-container-registry-with-harbor for an authorized cybersecurity task."
policy:
  allow_implicit_invocation: false
