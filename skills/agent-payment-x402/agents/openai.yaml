interface:
  display_name: "Agent Payment X402"
  short_description: "Add x402 payment execution to AI agents with per-task budgets..."
  default_prompt: "Use $agent-payment-x402 to help with this task."
policy:
  allow_implicit_invocation: true
