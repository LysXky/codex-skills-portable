interface:
  display_name: "Database Migrations"
  short_description: "Database migration best practices for schema changes, data mi..."
  default_prompt: "Use $database-migrations to help with this task."
policy:
  allow_implicit_invocation: true
