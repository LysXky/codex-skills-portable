interface:
  display_name: "Redis Patterns"
  short_description: "Redis data structure patterns, caching strategies, distribute..."
  default_prompt: "Use $redis-patterns to help with this task."
policy:
  allow_implicit_invocation: true
