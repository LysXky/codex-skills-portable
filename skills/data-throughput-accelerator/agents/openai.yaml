interface:
  display_name: "Data Throughput Accelerator"
  short_description: "Use when large data ingestion, backfill, export, ETL, warehou..."
  default_prompt: "Use $data-throughput-accelerator to help with this task."
policy:
  allow_implicit_invocation: true
