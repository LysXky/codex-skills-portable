interface:
  display_name: "Detecting Rootkit Activity"
  short_description: "Detects rootkit presence on compromised systems by identifyin..."
  default_prompt: "Use $detecting-rootkit-activity for an authorized cybersecurity task."
policy:
  allow_implicit_invocation: false
